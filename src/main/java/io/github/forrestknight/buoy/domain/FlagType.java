package io.github.forrestknight.buoy.domain;

/**
 * Flag value type. Only BOOLEAN exists in v1; STRING/NUMBER/JSON multivariate
 * flags are reserved future work (task bank #3).
 */
public enum FlagType {
    BOOLEAN
}
